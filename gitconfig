[user]
	email = yukio.siraichi@gmail.com
	name = Yukio Siraichi
[core]
	editor = nvim
[alias]
	co = checkout
	br = branch
	st = status
	undo = checkout HEAD --
	graph = log --graph --format=format:'%C(yellow)%h %C(cyan)%ad %C(reset)%s -- %C(yellow)%an%C(red)%d' --date=short
	head = log -3
