
# ls
alias ls='ls --color=auto'
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'

# grep
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'

# nvim
alias vim='nvim'
alias vi='nvim'

# APT No-Install-Recommends
alias apt='apt -o APT::Install-Recommends=no'
